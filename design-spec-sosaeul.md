# Design Spec — 보그헤어 소새울역점 (v2)

> 레퍼런스: 벨벳 마케팅 템플릿 (웜 토프 + 크림 + 테라코타, 하이컨트라스트 세리프)
> **이 문서는 기존 소새울역점 디자인 규칙을 완전히 대체합니다.** 이전 스펙을 참조하지 마세요.
> `[높음]` 시안에서 명확 / `[중간]` 추정 / `[파생]` 시안에 없어 설계함

---

## 0. 이 디자인의 정체성

이걸 잃으면 다른 디자인이 됩니다.

1. **거대한 세리프 숫자** — `1.` `2.` `3.` 이 제목보다 크게, 흐린 토프색으로. 이 페이지에서 가장 기억에 남는 요소.
2. **지그재그 + 배경 밴드 교차** — 텍스트/이미지 좌우가 번갈아 바뀌고, 동시에 섹션 배경이 크림/화이트로 번갈아 바뀝니다. 둘이 함께 움직여야 리듬이 생깁니다.
3. **로만 + 이탤릭 혼용 문장** — 헤드라인 안에서 강조 단어만 이탤릭 + 액센트 컬러. 서체 하나로 강약을 만드는 방식.
4. **하이컨트라스트 세리프의 압도적 크기** — 히어로 제목이 화면을 지배합니다. 소심하게 키우면 밋밋해집니다.

---

## 1. Color Tokens `[중간]`

```css
:root {
  /* Surface */
  --c-hero-veil:   #B9A996;  /* 히어로 사진 위 웜 토프 베일 */
  --c-cream:       #F2EDE7;  /* 교차 밴드 (짝수 섹션) */
  --c-white:       #FFFFFF;  /* 교차 밴드 (홀수 섹션) */
  --c-bg:          #FBF9F6;  /* 페이지 기본 */

  /* Accent */
  --c-accent:      #C58A5C;  /* 테라코타 — 솔리드 버튼, 이탤릭 강조, 체크마크 */
  --c-accent-deep: #A9703F;  /* hover */

  /* Line */
  --c-border:      #D8CFC5;
  --c-rule:        #C4B8AB;  /* 제목 밑 얇은 밑줄 */

  /* Text */
  --c-text:        #3D342C;  /* 제목 */
  --c-text-body:   #7A6E63;  /* 본문 */
  --c-numeral:     #A89684;  /* 대형 숫자 — 흐리게 */
  --c-on-hero:     #FFFFFF;
}
```

**액센트는 절제해서 씁니다.** 솔리드 버튼 / 이탤릭 강조어 / 체크마크 — 이 셋 외에는 사용 금지.
링크 색, 보더, 배경에 테라코타를 넣지 마세요. 남발하면 싸구려로 보입니다.

---

## 2. Typography `[높음]`

| 역할 | 성격 | 웹폰트 후보 |
|---|---|---|
| Display | 하이컨트라스트 세리프, 획 대비 큼 | Prata / Playfair Display 400 / Cormorant 500 |
| Display Italic | 위와 같은 패밀리의 이탤릭 | Playfair Display Italic |
| Body / UI | 라이트 산세리프, 작게 | Inter 300–400 / Jost 300 |

### Scale (데스크톱 1024px+ 기준)

```css
:root {
  --fs-hero:     88px;  /* 히어로 제목 — 2줄 전제 */
  --fs-hero-sub: 11px;  /* 히어로 부제, 대문자 */
  --fs-section:  40px;  /* SERVICES 등 섹션 타이틀, 대문자 */
  --fs-lead:     34px;  /* 스테이트먼트 헤드라인 */
  --fs-numeral:  72px;  /* 1. 2. 3. */
  --fs-h3:       26px;  /* 항목 제목 */
  --fs-eyebrow:  10px;  /* 대문자 아이브로우 */
  --fs-body:     13px;
  --fs-check:    11px;
  --fs-btn:      10px;

  --ls-hero:     0.01em;
  --ls-section:  0.08em;
  --ls-eyebrow:  0.20em;  /* 아이브로우는 자간을 아주 넓게 */
  --ls-btn:      0.10em;

  --lh-hero:     1.05;
  --lh-lead:     1.35;
  --lh-body:     1.75;
}
```

### 규칙

- 히어로 제목, 섹션 타이틀, 아이브로우, 버튼 라벨 → 대문자.
- 스테이트먼트 헤드라인은 **중앙정렬**, 강조어만 `<em>` + `--c-accent`.
- 항목 제목 아래 **1px 밑줄** (`--c-rule`), 폭은 제목 블록의 60~70%.
- 본문은 좌측정렬. (요가 스펙의 justify 규칙은 폐기)

### 한글 적용 `[파생]`

시안은 라틴 하이컨트라스트 세리프가 핵심인데 한글엔 대응 서체가 드뭅니다.

- 히어로: **한글 제목 + 라틴 부제** 구조 권장. 한글은 Gowun Batang / 나눔명조, 자간 `0.02em` 이하.
- `SERVICES` 같은 섹션 타이틀은 **라틴 그대로 유지**하고 그 아래 한글 부제를 다는 편이 시안 톤에 가깝습니다.
- 이탤릭 강조는 한글에 적용하지 마세요. 한글 이탤릭은 기울인 것처럼 보일 뿐입니다. 대신 **액센트 컬러만** 적용.

---

## 3. Layout `[높음]`

```css
:root {
  --container:   1120px;
  --pad-x-lg:    64px;
  --pad-x-md:    40px;
  --pad-x-sm:    20px;
  --sec-y-lg:    104px;
  --sec-y-sm:    56px;

  /* Measure — .container(가로 "위치")와 별개인, 텍스트 블록의 읽기 좋은 줄 길이 */
  --measure-lead: 620px;  /* 대형 헤드라인 */
  --measure-body: 560px;  /* 본문 문단 */
}
```

- **`.container` 규칙 유지** (응암역점에서 확정한 것과 동일):
  모든 섹션 콘텐츠는 `.container`로 감싸고, 개별 요소에 `width` / `max-width` / `margin: auto`를 직접 지정하지 않습니다.
- 히어로와 배경 밴드만 full-bleed. 내부 텍스트는 `.container` 기준.

### Measure (읽기 폭)

`.container`와 measure는 다른 개념입니다. **`.container`는 가로 "위치"**(섹션 내 정렬 기준, full-bleed 배경 안에서 콘텐츠가 어디 놓이는지)를 통일하는 것이고, **measure는 텍스트 블록의 읽기 좋은 줄 "길이"**를 잡는 것입니다. 헤드라인이나 본문을 `.container`(1120px) 폭 그대로 두면 줄이 너무 길어져 읽기 어려우므로, 별도 유틸리티 클래스로 좁힙니다.

```css
.measure-lead { max-width: var(--measure-lead); margin-inline: auto; }
.measure-body { max-width: var(--measure-body); margin-inline: auto; }
```

- 헤드라인(스테이트먼트, 섹션 리드 카피 등) → `.measure-lead`
- 본문 문단 → `.measure-body`
- 아이브로우(라벨류)는 폭 제한 없이 중앙정렬만 유지

**폭 제어는 `.container` / `.measure-lead` / `.measure-body` 이 세 클래스로만 합니다.** 개별 선택자에 `max-width` 숫자를 직접 넣는 것은 금지입니다(§7 금지 목록에도 반영). 이 세 토큰에 없는 새로운 폭 값이 필요하면 임의로 만들지 말고 질문하세요.

### 지그재그 그리드

```
[홀수 항목]                        [짝수 항목]
┌─────────────┬───────────┐      ┌───────────┬─────────────┐
│ 1. 제목      │           │      │           │ 2. 제목      │
│    본문      │   이미지   │      │   이미지   │    본문      │
│    체크리스트 │           │      │           │    체크리스트 │
│    버튼      │           │      │           │    버튼      │
└─────────────┴───────────┘      └───────────┴─────────────┘
   46%            42%                 42%           46%
        gap 12%                          gap 12%
```

배경 밴드도 함께 교차: 1번 크림 → 2번 화이트 → 3번 크림.

---

## 4. Components

### 4.1 Hero `[높음]`

- 인물 사진 full-bleed, 위에 `--c-hero-veil` 반투명 오버레이 (`opacity: 0.55` 내외).
- 상단 바: 좌측 브랜드명(대문자 세리프, 자간 넓게) / 우측 네비(대문자 산세리프 10px).
- 중앙: 대형 세리프 제목 2줄 → 대문자 부제 1줄 → 아웃라인 pill 버튼.
- 높이: `min-height: 92vh`.
- 사진은 **인물이 중앙보다 약간 위**에 오도록 `object-position` 조정. 제목이 얼굴을 가리지 않게.

### 4.2 대형 숫자 (Signature) `[높음]`

```css
.item-num {
  font-family: var(--ff-display);
  font-size: var(--fs-numeral);
  color: var(--c-numeral);
  line-height: 1;
}
```

- 제목 첫 줄과 **baseline이 아니라 상단이 맞도록** 배치.
- 숫자 뒤 마침표 포함 (`1.`).
- 제목 좌측에 별도 컬럼으로. 텍스트 흐름에 인라인으로 넣지 마세요.

### 4.3 버튼 `[높음]`

```css
.btn { height: 36px; padding: 0 20px; border-radius: 2px;
       font-size: var(--fs-btn); letter-spacing: var(--ls-btn);
       text-transform: uppercase; }
.btn--outline { border: 1px solid var(--c-accent); background: transparent; color: var(--c-accent); }
.btn--solid   { border: 1px solid var(--c-accent); background: var(--c-accent); color: #fff; }
```

- 두 개를 **가로로 나란히**, gap 10px. (예: `자세히 보기` + `예약하기`)
- 예약 CTA는 항상 solid, 보조 액션은 outline.
- 히어로 버튼만 예외: `border-radius: 999px`, 흰색 아웃라인.

### 4.4 체크리스트 `[높음]`

```
✓  콘텐츠 기획 및 발행
✓  커뮤니티 관리
✓  월간 리포트
```

- 체크마크 색 `--c-accent`, 텍스트 `--c-text-body` 11px.
- 항목 3개 고정. 늘리면 균형이 깨집니다.

### 4.5 이미지 `[중간]`

- 세로형 비율 (약 4:5), `border-radius: 0`.
- **짝수 항목에만 오프셋 프레임**: 이미지 뒤로 8px 어긋난 1px 보더 사각형. 시안 2번 항목의 디테일입니다.
- 톤: 웜하게. `filter: saturate(0.9)`.

---

## 5. 소새울역점 콘텐츠 매핑 `[파생]`

시안 구조를 지점 콘텐츠에 대응시킨 안입니다.

| 시안 섹션 | 소새울역점 대응 |
|---|---|
| Hero | 지점 대표 이미지 + 지점명 + 예약 CTA |
| Statement | 지점 소개 한 문장 (강조어에 액센트) |
| Services 1·2·3 | **디자이너 3인** — 기존 인물 사진 그대로 지그재그 배치 |
| 체크리스트 | 디자이너별 시술 특기 3개 |
| solid 버튼 | 기존 **디자이너별 네이버 예약 링크** 그대로 연결 |
| Portfolio 3-grid | 시술 전후 / 스타일 갤러리 |
| (추가) | 가격표, 오시는 길 — 응암역점에서 쓴 컨테이너 규칙 그대로 |

디자이너 3명이 시안의 1·2·3 구조와 정확히 맞아떨어집니다. 지그재그 배치가 인물 사진과 특히 잘 어울립니다.

---

## 6. Responsive `[파생]`

⚠️ **원본 시안은 데스크톱 전용입니다.** 아래 모바일 규칙은 파생 설계입니다.

| 요소 | ~767px | 768–1023px | 1024px~ |
|---|---|---|---|
| 컨테이너 | 100% | 720px | 1120px |
| 좌우 패딩 | 20px | 40px | 64px |
| 섹션 상하 | 56px | 76px | 104px |
| 히어로 제목 | 40px | 60px | 88px |
| 히어로 높이 | 78vh | 85vh | 92vh |
| 지그재그 | **전부 1열 스택** | 1열 스택 | 좌우 교차 |
| 스택 순서 | 숫자+제목 → 이미지 → 본문 → 버튼 | 동일 | — |
| 대형 숫자 | 40px, 제목과 같은 줄 | 56px | 72px |
| 버튼 2개 | 가로 유지 (각 50%) | 가로 | 가로 |
| 배경 밴드 교차 | 유지 | 유지 | 유지 |
| 포트폴리오 그리드 | 2열 | 2열 | 3열 |

**모바일 지그재그 처리가 핵심입니다.** 좌우 교차를 그대로 스택하면 이미지가 위였다 아래였다 하며 리듬이 깨집니다. 모바일에서는 **모든 항목이 동일한 순서**로 쌓여야 합니다.

### 품질 기준

- 터치 타깃 최소 44px — 버튼 36px이므로 **모바일에서는 44px로 상향**.
- `--c-numeral` on `--c-cream` 대비비 낮음 → 장식 전용, 스크린리더에서 `aria-hidden`.
- 본문 `--c-text-body` on `--c-cream` ≈ 4.7:1 (AA 통과).
- 히어로 텍스트는 베일 오버레이 위에 있으므로 대비비 실측 필요.

---

## 7. 금지 목록

- `box-shadow` (오프셋 프레임은 보더로 구현, 그림자 아님)
- `linear-gradient` — 히어로 베일은 단색 반투명
- 테라코타를 §1에 명시된 3곳(솔리드 버튼·이탤릭 강조·체크마크) 외에 사용
- `border-radius` 4px 초과 (히어로 pill 버튼만 예외)
- 한글에 `font-style: italic` 적용
- 모바일에서 지그재그 좌우 교차 유지
- 토큰에 없는 색상·폰트사이즈·여백 임의 생성
- 개별 선택자에 `max-width` 숫자를 직접 지정 (폭 제어는 `.container` / `.measure-lead` / `.measure-body` 세 클래스로만, §3 Measure 참고)
- 이전 소새울역점 디자인 규칙 참조
